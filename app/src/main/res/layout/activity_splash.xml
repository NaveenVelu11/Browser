<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- Center Content -->
    <LinearLayout
        android:id="@+id/splash_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Minimal flat logo container — no shadow, no gradient -->
        <LinearLayout
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:background="@drawable/bg_splash_logo"
            android:gravity="center">

            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/splash_logo"
                android:layout_width="40dp"
                android:layout_height="40dp"
                app:srcCompat="@drawable/ic_deerone_logo"
                android:contentDescription="@string/app_name"
                android:scaleType="fitCenter" />
        </LinearLayout>

        <!-- App Name — clean weight, tighter tracking -->
        <TextView
            android:id="@+id/splash_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:text="DeerOne"
            android:textColor="#1C1C1E"
            android:textSize="26sp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="-0.01"
            android:textStyle="bold" />

        <!-- Minimal tagline -->
        <TextView
            android:id="@+id/splash_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Simple. Fast. Private."
            android:textColor="#8E8E93"
            android:textSize="13sp"
            android:fontFamily="sans-serif"
            android:letterSpacing="0.02" />

    </LinearLayout>

    <!-- Bottom version label only — no spinner -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="40dp"
        android:text="v1.0"
        android:textColor="#C7C7CC"
        android:textSize="11sp"
        android:fontFamily="sans-serif"
        android:letterSpacing="0.04" />

</RelativeLayout>
